{
  "builds": [
    {
      "src": "/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"
    }
  ],

  "buildCommand": "npm install && cd prisma && npx prisma generate && cd .."
}
